# HawkEye – Python dependencies
# Install with: pip install -r requirements.txt

# GUI framework
PySide6>=6.5.0

# HTTP requests (used by backend_bridge.py and reverse_image_search.py)
requests>=2.31.0

# HTML parsing (used by reverse_image_search.py for Google/Yandex/Bing scraping)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Perceptual image hashing for similarity scoring (used by reverse_image_search.py)
# Falls back gracefully if not installed, but similarity scores will be unavailable
Pillow>=10.0.0

# Jupyter notebook support (used by POC.ipynb)
notebook>=7.0.0
ipykernel>=6.25.0
